type Query {
  helloWorld: String!
  getPersonByName(name: String!): Person!
  getCarByOwner(name: String!, age: Int!, job: String): Car!
  getHikingTrailByDifficulty(difficulty: DificultyRanking!): HikingTrail!
  getHikingTrailsByRating(rating: Int): HikingTrail!
}

type Person {
  name: String!
  age: Int!
  job: String
}

type Car {
  owner: Person!
  licencePlate: String!
  colour: String
}

enum DificultyRanking {
  BEGGINER
  INTERMEDIATE
  HARD
  EXPERT
}

type HikingTrail {
  distance: Float!
  elevation: Float!
  difficulty: DificultyRanking!
  parking: Boolean
  rating: Int
}
